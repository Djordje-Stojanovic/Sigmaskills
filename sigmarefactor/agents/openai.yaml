interface:
  display_name: SigmaRefactor
  short_description: Review and simplify large files safely
  default_prompt: Use $sigmarefactor to inspect the five largest source files, explain safe refactoring choices, and wait for agreement before editing.
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
